import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#556cd6",
    },
    secondary: {
      main: "#e68027",
    },
    error: {
      main: "#d12121",
    },
    background: {
      default: "#93c8eb",
    },
  },
});

export default theme;
